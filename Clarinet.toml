[project]
name = 'Blockchain-Based-Enterprise-Resource-Planning-Mobile-Integration-System'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.mobile-app-contract]
path = 'contracts/mobile-app-contract.clar'
clarity_version = 2
epoch = 2.5

[contracts.security-coordination]
path = 'contracts/security-coordination.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
